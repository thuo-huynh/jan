# apps/web/app/(app)/boards

Kanban board list + `[boardId]` detail route. Backed by `features/kanban/` (drag-and-drop via
`@dnd-kit`). This is also the app's post-login landing route (`AppLayout`'s logo links here).

See the root [`CLAUDE.md`](../../../../../CLAUDE.md) for project-wide rules — this file only
notes what's local to this folder.
