# apps/web/app/admin/stats

Usage stats dashboard (total/active users, tasks, etc.) — calls `GET /api/admin/stats`. Purely
read-only display; no mutation endpoints in this sub-route.

See the root [`CLAUDE.md`](../../../../../CLAUDE.md) for project-wide rules — this file only
notes what's local to this folder.
